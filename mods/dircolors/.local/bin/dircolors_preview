#!/bin/bash

{
  cat "${1:-${XDG_CONFIG_HOME:-$HOME/.config}/dircolors}" 2>/dev/null ||
  cat ~/.dircolors 2>/dev/null ||
  dircolors --print-database
} | \
  grep -P '^\w+\s+[\d;]+' | \
  awk -F' ' '{ print "\033[" $2 "m" $1 "\033[0m" }' | \
  column
